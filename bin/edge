#!/usr/bin/env node

var cli = require( '../' );

cli.dispatch( process.argv ).then( function ( output ) {
  console.log( output );
} ).catch( function ( err ) {
  console.error( err.message );
  console.error( err.payload );
  process.exit( 1 );
} );
